# 项目协作约束

每次提交、推送或创建 PR 前，必须先读取 `docs/pr-guidelines.md`，并按其中最新约束执行。

当前关键约束：

- 提交信息使用中文 Conventional Commits，例如 `feat: 新增异步叙事分析流程` 或 `fix: 修复本地环境变量加载顺序`。
- 推送说明、提交总结、PR 描述、后端应用日志和关键运行日志使用中文。
- 真实 `.env`、缓存、日志、构建产物、依赖目录和本地测试文本不得提交。
